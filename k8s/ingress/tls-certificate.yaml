# TLS Certificate Secret (replace with actual certificate data)
apiVersion: v1
kind: Secret
metadata:
  name: messaging-tls-secret
  labels:
    app: user-admin-messaging
type: kubernetes.io/tls
data:
  # Base64 encoded certificate and key
  # Replace these with actual certificate data
  tls.crt: LS0tLS1CRUdJTiBDRVJUSUZJQ0FURS0tLS0t...
  tls.key: LS0tLS1CRUdJTiBQUklWQVRFIEtFWS0tLS0t...
---
# Alternative: Use cert-manager for automatic certificate provisioning
# Uncomment the following if using cert-manager
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: messaging-certificate
#   labels:
#     app: user-admin-messaging
# spec:
#   secretName: messaging-tls-secret
#   issuerRef:
#     name: letsencrypt-prod
#     kind: ClusterIssuer
#   dnsNames:
#   - messaging.example.com