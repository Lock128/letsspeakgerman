apiVersion: v1
kind: Service
metadata:
  name: user-frontend-service
  labels:
    app: user-frontend-service
    component: frontend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: user-frontend-service