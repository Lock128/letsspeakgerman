apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: user-admin-messaging
    component: config
type: Opaque
data:
  # Redis password (base64 encoded)
  # Replace with actual base64 encoded password
  redis-password: cGFzc3dvcmQxMjM=  # password123
  
  # JWT secret for authentication (base64 encoded)
  # Replace with actual base64 encoded JWT secret
  jwt-secret: bXlfc2VjcmV0X2p3dF9rZXk=  # my_secret_jwt_key
  
  # Database credentials if needed (base64 encoded)
  # db-username: dXNlcm5hbWU=  # username
  # db-password: cGFzc3dvcmQ=  # password
  
  # API keys or other sensitive configuration (base64 encoded)
  # api-key: bXlfYXBpX2tleQ==  # my_api_key