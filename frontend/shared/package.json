{
  "name": "user-admin-messaging-shared-config",
  "version": "1.0.0",
  "description": "Shared configuration system for user-admin messaging frontend applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}