{
  "name": "user-admin-messaging-lambda",
  "version": "1.0.0",
  "description": "Lambda functions for user-admin messaging application",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^18.19.130",
    "@types/redis": "^4.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1300.0",
    "redis": "^4.6.0"
  }
}
